routes/ → Define your API endpoints (CRUD for ledger entries).
controllers/ → Logic for handling requests.
models/ → Database schema (MySQL tables).
services/ → Any helpers, e.g., OCR processing.
config/ → Database connection and other configs.
